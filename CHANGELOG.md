# 📋 EmmArk 小秘書 更新日誌

記錄系統做過的調整，最新在最上面。

---

## 2026-06-24

### 🎉 新功能
- **查詢選單**：打 `選單`，所有查閱型指令變按鈕，點一下直接查。
- **待辦清單**：打 `待辦`，列出所有未完成（逾期標 ⚠️），每項可直接點 ✅ 完成。
- **更新日誌**：打 `更新` 看最近調整（完整見 CHANGELOG.md）。
- **準時提醒**：到點前幾分鐘就先發（預設提前 5 分鐘，可用 `REMIND_LEAD_MINUTES` 調整），不再「當下才提醒來不及」。
- **互動提醒卡片**：提醒跳出時可直接點「✅ 完成 / ⏰ 延後 1 小時 / 📅 改明天」。
- **重複提醒**：`提醒 每天/每週三/每月5號 + 時間`，響過自動排下一次。
- **相對時間**：支援「X 分鐘後 / 半小時後 / X 小時後 / X 天後」。
- **拍照建提醒**：傳海報/通知圖片，自動辨識時間建立提醒（需 AI 額度）。
- **每日早安簡報**：每天早上自動推今日任務＋今日服事。
- **萬年曆**：LINE `日曆`（本月＋下月格狀，含主日服事）＋ 後台月曆分頁。
- **服事表查詢**：`服事表`（我的）、`服事表 7/6`（當天全班表）、`8月服事表`（我那月）。
- **敬拜順序**：`寫順序 [日期] 內容` / `看順序 [日期]`，內容原樣保留（含「主→副」結構）。
- **順序範本**：`存範本` / `套範本 名稱 [日期]`（一鍵生成當天順序）/ `範本`。
- **詩歌資料庫**：`存歌` / `找歌` / `詩歌庫`。
- **主題式提醒**：`主題 名稱` 一次整批建立（如特會），`主題清單`（＝後台專案）/ `看主題` / `刪主題`。
- **連結收藏箱**：`收藏 網址`（或直接貼網址）/ `收藏清單`。
- **全文搜尋**：`搜尋 關鍵字`。
- **前綴指令**：`筆記` / `待辦` / `提醒`（不繞 AI，最穩）。
- **兩層指令選單**：打「指令」先出分類按鈕，點了再展開。

### 🎨 介面
- 後台改版為深綠品牌設計（卡片、陰影、配色）。
- 手機列表改卡片式、桌機列表固定欄寬，免左右捲動。
- 後台月曆併入主日服事（綠色 ⛪ 標記）。
- 設計版圖文選單（4×2 八格，含「指令」按鈕）。

### 🐛 修復
- 提醒不觸發（`is_reminded` 未設導致漏撈）。
- 列表「編輯」鈕無反應（長內容/換行打斷 onclick）。
- 圖片下載失敗（改直連 LINE 內容 API）。
- AI 漏抓時間 → 用內建解析補上，避免有時間的句子變筆記。

### ⚙️ 規則調整
- 專案更新類別**只記錄、不提醒**。
- 「主題」與後台「專案」統一為同一分類。

---

> 維護：之後每次調整都在最上面加一段。
